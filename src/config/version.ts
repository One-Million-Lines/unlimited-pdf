/**
 * Single source of truth for the extension version.
 * Kept separate so build scripts and runtime code share one constant.
 */
export const APP_VERSION = '0.1.0';

/** Product display names, reused by manifest and UI. */
export const PRODUCT_NAME = 'UnlimitedPDF — Private PDF Toolbox';
export const PRODUCT_SHORT = 'UnlimitedPDF';
